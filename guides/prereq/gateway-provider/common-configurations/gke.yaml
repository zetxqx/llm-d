gateway:
  gatewayClassName: gke-l7-regional-external-managed
  gatewayParameters:
    accessLogging: false

provider:
  name: gke

inferencePool:
  apiVersion: "inference.networking.x-k8s.io/v1alpha2"
