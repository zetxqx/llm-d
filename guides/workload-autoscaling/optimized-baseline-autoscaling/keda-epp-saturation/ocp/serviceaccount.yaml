# Dedicated ServiceAccount whose token KEDA borrows to authenticate to Thanos
# Querier. This path runs no WVA controller, so the guide provisions its own
# metrics reader (same pattern as keda-epp-queue/ocp).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: keda-epp-metrics-reader
