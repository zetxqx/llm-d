# Dedicated ServiceAccount whose token KEDA borrows to authenticate to Thanos
# Querier. This path runs no WVA controller, so the queue guide provisions its
# own metrics reader (mirrors the WVA keda/ocp overlay, which borrows the
# controller-manager SA token).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: keda-epp-metrics-reader
