# Moved — now `keda-epp-queue`

This guide moved to **[keda-epp-queue/README.md](keda-epp-queue/README.md)**.

The old name `README.hpa-epp.md` described the **queue-based KEDA + EPP**
autoscaling path (KEDA creates the HPA; it is not a hand-written HPA). It was
renamed to `keda-epp-queue` to match its manifests
(`optimized-baseline-autoscaling/keda-epp-queue/`) and the sibling
`keda-epp-saturation` path. This stub remains only for inbound-link compatibility.
