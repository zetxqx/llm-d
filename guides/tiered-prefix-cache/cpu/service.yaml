apiVersion: v1
kind: Service
metadata:
  name: qwen-model-service
  namespace: llm-d-cpu-offloading
spec:
  type: ClusterIP
  selector:
    app: qwen-model-32b  # Matches the selector in your deployment
  ports:
    - protocol: TCP
      port: 80           # The port the Service exposes internally
      targetPort: 8000   # The port your container is listening on
