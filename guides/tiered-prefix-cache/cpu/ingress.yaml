apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qwen-model-ingress
  namespace: llm-d-cpu-offloading
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: qwen-model-service
            port:
              number: 80