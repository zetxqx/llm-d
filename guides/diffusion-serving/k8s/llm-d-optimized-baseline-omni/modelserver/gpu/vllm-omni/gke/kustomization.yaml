apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# GKE overlay: same as base, plus disables the GKE NCCL tuner init (harmless for
# single-GPU diffusion, kept for parity with the upstream guide's gke overlay).
resources:
  - ../base
components:
  - ../../../../../../llm-d/guides/recipes/modelserver/components/disable-gke-nccl-tuner-patch
