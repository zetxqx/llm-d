apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# GKE overlay: base + disable the GKE NCCL tuner init (parity with the
# upstream guide's gke overlay; harmless for single-GPU TTS).
resources:
  - ../base
components:
  - ../../../../../../../recipes/modelserver/components/disable-gke-nccl-tuner-patch
