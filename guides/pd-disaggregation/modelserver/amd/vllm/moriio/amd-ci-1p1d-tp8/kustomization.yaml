apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Inherit the amd-ci leaf (base 4P/4D TP=2 + amd-ci image/env/NAD/model-cache).
  # This variant only layers the 1P/1D TP=8 overrides on top.
  - ../amd-ci

patches:
  - path: patch-1p1d-tp8.yaml
