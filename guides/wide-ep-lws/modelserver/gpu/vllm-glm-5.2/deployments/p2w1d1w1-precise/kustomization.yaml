apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# GLM-5.2-FP8 precise routing: 2 prefill nodes (DEP8 each) + 1 decode node
# (DEP8) -> 3 nodes / 24 GPUs.
resources:
  - ../p2w1d1w1
  - ../../../../../render
components:
  - ../../components/kv-events
patches:
  - path: delete-evictor.yaml
