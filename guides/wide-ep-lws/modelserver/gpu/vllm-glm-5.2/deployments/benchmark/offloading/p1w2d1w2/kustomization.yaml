apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# offloading: 1p×2w + 1d×2w -> 4 nodes / 32 GPUs.
resources:
  - ../../../../providers/coreweave
components:
  - ../../../../components/no-mtp
  - ../../../../components/offloading-tiered
patches:
  - target:
      kind: LeaderWorkerSet
      name: ".*-prefill"
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
      - op: replace
        path: /spec/leaderWorkerTemplate/size
        value: 2
  - target:
      kind: LeaderWorkerSet
      name: ".*-decode"
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
      - op: replace
        path: /spec/leaderWorkerTemplate/size
        value: 2
