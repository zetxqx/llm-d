## Predicted Latency-Based Scheduling — routing-only setup.
##
## Overrides the chart's default plugin pipeline only to raise the
## approx-prefix-cache-producer maxPrefixTokensToMatch (the chart default leaves it
## unset). The predictor trains on end-to-end latency; no SLO headers are
## required. For SLO-aware scheduling (x-llm-d-slo-ttft-ms / x-llm-d-slo-tpot-ms,
## streamingMode: true), use predicted-latency-slo.values.yaml.

router:
  latencyPredictor:
    enabled: true
  extraServicePorts:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8081
  epp:
    pluginsConfigFile: "predicted-latency-plugins.yaml"
    pluginsCustomConfig:
      predicted-latency-plugins.yaml: |
        apiVersion: llm-d.ai/v1alpha1
        kind: EndpointPickerConfig
        plugins:
        - type: approx-prefix-cache-producer
          parameters:
            maxPrefixTokensToMatch: 131072
        - type: predicted-latency-producer
        - type: prefix-cache-affinity-filter
          name: loose-affinity-filter
          parameters:
            affinityThreshold: 0.8
        - type: latency-scorer
        - type: weighted-random-picker
        schedulingProfiles:
        - name: default
          plugins:
          - pluginRef: predicted-latency-producer
          - pluginRef: loose-affinity-filter
          - pluginRef: latency-scorer
          - pluginRef: weighted-random-picker

  modelServers:
    matchLabels:
      llm-d.ai/guide: "optimized-baseline"
