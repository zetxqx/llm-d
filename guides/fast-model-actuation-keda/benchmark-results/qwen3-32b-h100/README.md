# EPP+KEDA+FMA Benchmark Report

Three actuation paths for **`Qwen/Qwen3-32B`**, measured under the KEDA
[saturation-based][guide] and [queue-based][queue-guide] autoscaling trigger
modes, with load generated by **[inference-perf][inference-perf]**. Both trigger
modes compare the same three passes, which differ only in **who owns the
model-server lifecycle**:

[inference-perf]: https://github.com/kubernetes-sigs/inference-perf

| Pass | Model load |
|---|---|
| **Baseline** | Cold — a new decode pod loads the model on every scale-up |
| **Warm** | FMA launcher already running; scale-up creates a new vLLM instance |
| **Hot** | All replicas pre-loaded during standup, then scaled to 1 (vLLM sleeps); scale-up wakes a sleeping instance |

Run on an NVIDIA H100-80GB-HBM3 OpenShift cluster, one GPU per replica. Every
metric reported below is defined in [Metric Definitions](#metric-definitions).

> [!NOTE]
> Values in the results tables are **3-run averages**, so small differences
> between columns should not be read as significant. Within each trigger mode
> all three passes used identical workload, model, and autoscaling
> configuration — only the model-server lifecycle owner differs.

## Saturation-Based Autoscaling

These three passes use the autoscaling setup from
[keda-epp-saturation][guide] — KEDA polling the EPP
`llm_d_epp_flow_control_pool_saturation` and `llm_d_epp_request_running` gauges,
EPP `flowControl` feature gate on, the optimized-baseline scheduler plugins, and
a KEDA-generated HPA on the scale target.

[guide]: ../../../workload-autoscaling/keda-epp-saturation/README.md

> 4,256 input tokens (4,000-token shared prefix + 256-token question) · 256
> output tokens · 2 client workers, constant arrival — 0.5 RPS for 120 s then
> 2 RPS for 900 s · 1,860 requests over a ~1,034 s window

### Configuration

The [keda-epp-saturation][guide] configuration, with `maxReplicaCount` lowered
to 6 and HPA scale-up/scale-down policies added. Thresholds, polling interval,
trigger queries, metric source, and plugin set are the guide's defaults.

| Component | Parameter | Value |
|---|---|---|
| Model | Name / max length | `Qwen/Qwen3-32B` / 16000 |
| Model | GPU memory utilization | 0.95 |
| Model | Tensor parallelism | 1 (1 GPU per replica) |
| KEDA | Trigger 1 — pool saturation threshold | `0.7` |
| KEDA | Trigger 2 — running requests threshold | `16` |
| KEDA | Polling interval | 15 s |
| HPA | Min / Max replicas | 1 / 6 |
| HPA | Scale-up | 0 s stabilization, 1 Pod / 30 s |
| HPA | Scale-down | 300 s stabilization, 1 Pod / 300 s |
| Workload | Load generator | `inference-perf` |
| Idle replicas | Baseline / Warm / Hot | 1 decode pod / 1 requester / 6 requesters pre-loaded, then scaled to 1 |

### Results

| Metric | Baseline | Warm | Hot | Δ% Hot vs Baseline |
| :------------------------------------- | -------: | -------: | -------: | -----: |
| Duration (s)                           | 1,033.7  | 1,034.0  | 1,039.3  | +0.5%  |
| Total requests                         | 1,860    | 1,860    | 1,860    | —      |
| Mean latency (ms)                      | 20,076.6 | 18,687.0 | 11,663.5 | −41.9% |
| P95 latency (ms)                       | 77,992.9 | 64,873.4 | 22,838.2 | −70.7% |
| Mean TTFT (ms)                         | 12,341.7 | 10,938.2 | 2,960.1  | −76.0% |
| P95 TTFT (ms)                          | 67,894.7 | 55,368.1 | 13,423.7 | −80.2% |
| Mean ITL (ms/tok)                      | 26.6     | 27.5     | 30.3     | +14.0% |
| P95 ITL (ms/tok)                       | 31.0     | 30.2     | 32.8     | +6.0%  |
| Avg replicas                           | 3.94     | 3.76     | 2.52     | −36.0% |
| Max replicas                           | 6.0      | 5.3      | 3.0      | −50.0% |
| Avg KV cache utilization               | 32.7%    | 38.3%    | 55.7%    | +70.4% |
| Avg queue depth (EPP)                  | 4.7      | 3.5      | 0.9      | −80.0% |
| Avg flow-control pool saturation (EPP) | 1.18     | 0.98     | 0.75     | −36.3% |
| Avg flow-control queue (EPP)           | 11.9     | 9.6      | 1.8      | −85.2% |
| Avg running requests (EPP)             | 24.2     | 23.9     | 18.5     | −23.6% |
| Avg pod startup (s)                    | 155.0    | 85.7     | 4.0      | −97.4% |
| Hot hit rate                           | _n/a_    | 0.0%     | 100.0%   | —      |
| Warm hit rate                          | _n/a_    | 100.0%   | 0.0%     | —      |
| Failures                               | 63.3     | 19.3     | 0.0      | −100.0% |

Pod startup drives everything else: 155 s (baseline, cold model load) → 85.7 s
(warm, new vLLM on a live launcher) → 4.0 s (hot, wake a sleeping vLLM). With
**FMA hot-start, autoscaling relief arrives ~39× faster** — a scale-up decision
becomes serving capacity in 4 s instead of 155 s, so the new replica absorbs the
backlog while it is still building rather than after requests have already piled
up. Queues never build, and **P95 TTFT falls 80%** (67.9 s → 13.4 s) with mean
latency down 42% (20.1 s → 11.7 s).

That comes on **36% fewer replicas** (3.94 → 2.52): the hot path scales out
sooner, so it needs fewer pods to stay ahead of the load. It was also the only
pass to complete all 1,860 requests in every run — baseline averaged 63.3
failures, warm 19.3.

## Queue-Based Autoscaling

The same three passes under the [keda-epp-queue][queue-guide] setup, which
scales on EPP queue depth (`llm_d_epp_flow_control_queue_size`) rather than pool
saturation, with running requests as the second trigger.

[queue-guide]: ../../../workload-autoscaling/keda-epp-queue/README.md

### Configuration

The [keda-epp-queue][queue-guide] configuration, with `maxReplicaCount` lowered
to 6. Thresholds, polling interval, trigger queries, metric source, and plugin
set are the guide's defaults. Model and workload are unchanged from the
saturation section.

| Component | Parameter | Value |
|---|---|---|
| KEDA | Trigger 1 — queue size threshold | `1` |
| KEDA | Trigger 2 — running requests threshold | `16` |
| KEDA | Polling interval | 15 s |
| HPA | Min / Max replicas | 1 / 6 |
| HPA | Scale-up | 0 s stabilization, 1 Pod / 30 s |
| HPA | Scale-down | 300 s stabilization, 1 Pod / 300 s |

### Results

| Metric | Baseline | Warm | Hot | Δ% Hot vs Baseline |
| :------------------------------------- | -------: | -------: | -------: | -----: |
| Duration (s)                           | 1,039.7  | 1,032.3  | 1,034.0  | −0.5%  |
| Total requests                         | 1,860    | 1,860    | 1,860    | —      |
| Mean latency (ms)                      | 20,969.9 | 18,284.9 | 11,311.7 | −46.1% |
| P95 latency (ms)                       | 84,187.9 | 70,992.7 | 33,514.3 | −60.2% |
| Mean TTFT (ms)                         | 13,033.7 | 10,739.7 | 3,268.8  | −74.9% |
| P95 TTFT (ms)                          | 74,161.3 | 61,196.9 | 24,080.7 | −67.5% |
| Mean ITL (ms/tok)                      | 27.9     | 26.9     | 28.1     | +0.6%  |
| P95 ITL (ms/tok)                       | 31.2     | 30.2     | 31.0     | −0.7%  |
| Avg replicas                           | 3.87     | 4.04     | 3.27     | −15.5% |
| Max replicas                           | 6.0      | 6.0      | 4.7      | −22.2% |
| Avg KV cache utilization               | 36.7%    | 35.1%    | 41.0%    | +11.7% |
| Avg queue depth (EPP)                  | 6.3      | 4.2      | 1.1      | −82.4% |
| Avg flow-control pool saturation (EPP) | 1.56     | 1.18     | 0.62     | −60.0% |
| Avg flow-control queue (EPP)           | 13.5     | 9.9      | 2.1      | −84.2% |
| Avg running requests (EPP)             | 24.9     | 23.1     | 17.4     | −30.3% |
| Avg pod startup (s)                    | 149.0    | 85.3     | 4.0      | −97.3% |
| Hot hit rate                           | _n/a_    | 0.0%     | 100.0%   | —      |
| Warm hit rate                          | _n/a_    | 100.0%   | 0.0%     | —      |
| Failures                               | 80.0     | 36.0     | 0.0      | −100.0% |

The same mechanism holds: pod startup 149.0 s → 85.3 s → 4.0 s, so with FMA
hot-start autoscaling relief again arrives ~37× faster, and **P95 TTFT falls
68%** (74.2 s → 24.1 s) with mean latency down 46% (21.0 s → 11.3 s). The hot
path also does it on **16% fewer replicas** (3.87 → 3.27) and was the only pass
to complete all 1,860 requests in every run — baseline averaged 80.0 failures,
warm 36.0.

## Metric Definitions

Shared by both trigger modes above. Which EPP gauge acts as the KEDA scale
trigger differs — `llm_d_epp_flow_control_pool_saturation` for saturation-based,
`llm_d_epp_flow_control_queue_size` for queue-based, with
`llm_d_epp_request_running` as the second trigger in both — but all gauges are
reported throughout.

| Metric | Definition |
|---|---|
| Duration | Wall-clock length of the benchmark window (s) |
| Total requests | Requests issued by the harness over the run |
| Mean / P95 latency | End-to-end request latency, request sent to last token (ms) — mean and 95th percentile; lower is better |
| Mean / P95 TTFT | Time-to-first-token (ms) — mean and 95th percentile; lower is better |
| Mean / P95 ITL | Inter-token latency between consecutive generated tokens (ms/token) — mean and 95th percentile; lower is better |
| Avg replicas | Mean ready pod count during the test window |
| Max replicas | Peak ready pod count; 6 means the `maxReplicaCount` ceiling was reached |
| Avg KV cache utilization | Mean GPU KV cache utilization across serving replicas |
| Avg queue depth (EPP) | Mean pending-request queue depth at the endpoint proxy |
| Avg flow-control pool saturation (EPP) | Mean `llm_d_epp_flow_control_pool_saturation`; >1.0 means the pool is overloaded and throttling |
| Avg flow-control queue (EPP) | Mean EPP flow-control queue size |
| Avg running requests (EPP) | Mean `llm_d_epp_request_running` — in-flight requests across the pool |
| Avg pod startup | Mean time for a new replica to become ready and serve (s) — lower is better |
| Hot hit rate | Share of run-phase FMA actuations that woke a sleeping vLLM (`wake`) |
| Warm hit rate | Share of run-phase FMA actuations that created a new vLLM on an existing launcher (`create_instance`) |
| Failures | Requests that did not complete successfully |
| Δ% Hot vs Baseline | Relative change from the Baseline column to the Hot column; sign follows the raw value, so a negative delta is an improvement for the latency, queue, and startup metrics |
